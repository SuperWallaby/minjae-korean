import { worksheetSiteFooter } from "@/lib/worksheetSite/brand";

/** Fixed canvas for export (US Letter @ ~100dpi). */
export const WORKSHEET_WIDTH = 850;
export const WORKSHEET_HEIGHT = 1100;

export const WORKSHEET_FOOTER = worksheetSiteFooter();
